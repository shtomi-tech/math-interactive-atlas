name: Deploy to GitHub Pages

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Verify Pages input files
        run: |
          test -f atlas.html
          test -f index.html
          test -f static/atlas.css
          test -f static/atlas/main.js
          test -f static/atlas/content-data.json
      - name: Prepare static files
        run: |
          mkdir -p _site/static
          cp atlas.html index.html _site/
          cp static/atlas.css _site/static/atlas.css
          cp -R static/atlas _site/static/atlas
          touch _site/.nojekyll
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
