import { derivativeValue, polynomialValue, secantSlope, tangentLine } from "../../math/calculus.js?v=20260913-8a";
import { COLORS, clampParameter, createState, format } from "./common.js?v=20260913-8a";

export function mountSecant(context, config) { const state = createState(config, { a: 1, h: 1 }); const coefficients = [0, 0, 1]; const f = (x) => x * x; context.graph(f); context.graph((x) => { const slope = secantSlope(coefficients, state.a, state.h); return slope === null ? 0 : f(state.a) + slope * (x - state.a); }, { strokeColor: COLORS.secondary }); context.graph((x) => 2 * state.a * x - state.a ** 2, { strokeColor: COLORS.highlight, dash: 2 }); context.point([() => state.a, () => f(state.a)], { name: "P", fillColor: COLORS.highlight }); context.point([() => state.a + state.h, () => f(state.a + state.h)], { name: "Q", fillColor: COLORS.secondary }); const notify = () => { const slope = secantSlope(coefficients, state.a, state.h); config.onStateChange?.({ ...state }, `割線の傾き=${slope === null ? "h=0では未定義" : format(slope)} ／ 接線の傾き=2a=${format(2 * state.a)}`); context.board?.update(); }; notify(); return { setParameter(name, value) { if (config.parameters?.[name]) state[name] = clampParameter(config, name, value, { min: -3, max: 3 }); notify(); }, reset() { Object.assign(state, config.initial || { a: 1, h: 1 }); notify(); }, getState: () => ({ ...state }) }; }
export function mountDerivativeAtPoint(context, config) { const state = createState(config, { functionName: "square", a: 1 }); const buttons = []; const functions = { square: [0, 0, 1], cubic: [0, -3, 0, 1] }; const f = (x) => polynomialValue(functions[state.functionName], x) ?? 0; context.graph(f); context.graph((x) => derivativeValue(functions[state.functionName], x) ?? 0, { strokeColor: COLORS.secondary, dash: 2 }); context.graph((x) => { const current = tangentLine(functions[state.functionName], state.a); return current ? current.slope * x + current.intercept : 0; }, { strokeColor: COLORS.highlight }); context.point([() => state.a, () => f(state.a)], { name: "a", fillColor: COLORS.highlight }); ["square", "cubic"].forEach((name) => { const item = context.button(name === "square" ? "x²" : "x³−3x", state.functionName === name, () => { state.functionName = name; buttons.forEach((button, index) => button.setAttribute("aria-pressed", String(["square", "cubic"][index] === name))); notify(); }); buttons.push(item); }); function notify() { const slope = derivativeValue(functions[state.functionName], state.a) ?? 0; config.onStateChange?.({ ...state }, `${state.functionName === "square" ? "f=x²" : "f=x³−3x"} ／ f'(${format(state.a)})=${format(slope)} ／ 接線の傾き=${format(slope)}`); context.board?.update(); } notify(); return { setParameter(name, value) { if (name === "a") state.a = clampParameter(config, name, value, { min: -3, max: 3 }); notify(); }, reset() { Object.assign(state, config.initial || { functionName: "square", a: 1 }); notify(); }, getState: () => ({ ...state }) }; }
export function mountFunctionDerivative(context, config) { const state = createState(config, { a: 0 }); const coefficients = [0, -3, 0, 1]; const f = (x) => polynomialValue(coefficients, x) ?? 0; const derivative = (x) => derivativeValue(coefficients, x) ?? 0; context.graph(f); context.point([() => state.a, () => f(state.a)], { name: "f(a)", fillColor: COLORS.primary }); const secondary = context.secondaryBoard?.(); if (secondary) { secondary.create("functiongraph", [derivative, -6, 6], { strokeColor: COLORS.secondary, strokeWidth: 3, fixed: true, highlight: false }); secondary.create("point", [() => state.a, () => derivative(state.a)], { name: "f'(a)", fillColor: COLORS.highlight, fixed: true, highlight: false }); secondary.create("text", [-5.6, 5.2, () => `f'(x)=3x²−3 ／ f'(${format(state.a)})=${format(derivative(state.a))}`], { fixed: true, highlight: false, fontSize: 13, strokeColor: COLORS.secondary }); } const notify = () => config.onStateChange?.({ ...state }, `f(${format(state.a)})=${format(f(state.a))} ／ f'(${format(state.a)})=${format(derivative(state.a))} ／ ${derivative(state.a) > 0 ? "増加" : derivative(state.a) < 0 ? "減少" : "水平"}`); notify(); return { setParameter(name, value) { if (name === "a") state.a = clampParameter(config, name, value, { min: -3, max: 3 }); notify(); context.board?.update(); secondary?.update(); }, reset() { state.a = Number(config.initial?.a ?? 0); notify(); context.board?.update(); secondary?.update(); }, getState: () => ({ ...state }) }; }
export function mountCubicExtrema(context, config) { const state = createState(config, { k: 1 }); const f = (x) => x ** 3 - 3 * state.k * x; const derivative = (x) => 3 * x * x - 3 * state.k; context.graph(f); context.graph(derivative, { strokeColor: COLORS.secondary, dash: 2 }); const points = [context.point([0, 0], { name: "極値", fillColor: COLORS.highlight }), context.point([0, 0], { name: "極値", fillColor: COLORS.highlight })]; const notify = () => { const root = state.k > 0 ? Math.sqrt(state.k) : 0; const values = state.k > 0 ? [{ x: -root, y: f(-root) }, { x: root, y: f(root) }] : []; points.forEach((point, index) => { const item = values[index]; point?.setAttribute({ visible: Boolean(item) }); if (item) point.moveTo([item.x, item.y], 0); }); config.onStateChange?.({ ...state }, state.k > 0 ? `f'(x)=0：x=±${format(root)} ／ 左で極大 ${format(f(-root))}、右で極小 ${format(f(root))}` : "k≤0では極大・極小の組はありません"); context.board?.update(); }; notify(); return { setParameter(name, value) { if (name === "k") state.k = clampParameter(config, name, value, { min: -2, max: 3 }); notify(); }, reset() { Object.assign(state, config.initial || { k: 1 }); notify(); }, getState: () => ({ ...state }) }; }
export function mountIndefiniteIntegral(context, config) { const state = createState(config, { C: 0 }); context.graph((x) => x * x + state.C); context.graph((x) => 2 * x, { strokeColor: COLORS.secondary, dash: 2 }); context.text(-5.6, 5.2, () => `F(x)=x²+${format(state.C)} ／ F'(x)=2x`, { fontSize: 14, strokeColor: COLORS.primary }); const notify = () => config.onStateChange?.({ ...state }, `C=${format(state.C)} ／ 原始関数は上下に移動 ／ 導関数は2xで同じ`); notify(); return { setParameter(name, value) { if (name === "C") state.C = clampParameter(config, name, value, { min: -4, max: 4 }); notify(); context.board?.update(); }, reset() { Object.assign(state, config.initial || { C: 0 }); notify(); }, getState: () => ({ ...state }) }; }
export const calculusScenes = Object.freeze({ "secant-to-tangent": mountSecant, "derivative-at-point": mountDerivativeAtPoint, "function-and-derivative": mountFunctionDerivative, "cubic-extrema": mountCubicExtrema, "indefinite-integral": mountIndefiniteIntegral });
